#!/bin/bash

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd $SCRIPT_DIR

mkdir -p ../../build/home/yi/lib/ || exit 1

#rsync -av ./_install/* ../../build/home/yi/ || exit 1
cp ./_install/* ../../build/home/yi/lib

